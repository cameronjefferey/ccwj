# Archived tenancy docs

These documents describe the **v1** tenancy architecture (`(user_id, account_name)` and
`broker_account_id`). They are kept for historical context only.

**Active contract:** [`docs/V2_TENANT_KEY_DESIGN.md`](../V2_TENANT_KEY_DESIGN.md)

| Archived file | Superseded by |
|---------------|---------------|
| `BROKER_ACCOUNT_ID_MIGRATION.md` | v2 `tenant_id` design + SnapTrade-only broker path |
| `USER_ID_TENANCY_EXPLAINER.md` | v2 `tenant_id` design |

Do not implement new features against the patterns in these files.
